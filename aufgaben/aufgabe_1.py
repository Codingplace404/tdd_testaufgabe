from numbers import Real


def division(a: Real, b: Real) -> Real:
    # Aufgabe: diese Funktion soll zwei reelle Zahlen a und b 
    #   miteinander dividiert werden. Dabei ist a der Zähler und b der Nenner.
    #   zu reellen Zahlen zählen insbesondere auch Integer und Floats.
    #   Deine Aufgaben:
    #       a) returne das Ergebnis der Division zweier zahlen a / b
    #       b) runde das Ergebnis auf maximal 4 Nachkommastellen,
    #          Nutze hierzu die `round` Funktion.
    #       c) Wenn eine Zahl versehentlich ein String ist, returne `None`
    #       d) Wenn die Zahl b eine 0 ist, returne `None`
    pass
